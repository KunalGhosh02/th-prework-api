export enum USER_ROLES {
  STUDENT = 'student',
  TEACHER = 'teacher',
}

export const enum COLLECTIONS {
  STUDENTS = 'students',
  TEACHERS = 'teachers',
  COURSES = 'courses',
}

export const enum LOG_LEVEL {
  INFO = 'info',
  WARN = 'warn',
  ERROR = 'error',
  DEBUG = 'debug',
}
